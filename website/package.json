{
  "devDependencies": {
    "html-includes": "^5.0.0",
    "postcss-import": "^15.1.0",
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "build": "bundle-js ./_src/js/app.js -o ./_dist/app.js && minify ./_dist/app.js > ./_dist/app.min.js && npx tailwindcss -i input.css -o output.css --watch --minify",
    "compile": "html-includes --src index.html --dest index-min.html --minify",
    "tailwind": "npx tailwindcss -i input.css -o output.css --watch --minify"
  }
}
